import React, { useState } from "react"
import { useLocation } from "react-router-dom"

const StationsList = (props) => {
  // console.log(props)
  // console.log(props.lines[0].Stations[0].Name)
  let location = useLocation()


  return (
    <>
      <h1>Stations</h1>
      
      
    </>
)
}

export default StationsList